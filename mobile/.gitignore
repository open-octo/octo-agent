node_modules/
dist/
# Build artifact: the bundled web frontend (npm run bundle-web).
www/
# Generated by `npx cap add ios` / `add android` on a Mac. The custom plugin's
# reference source is committed under native/ instead.
ios/
android/
